language: c

addons:
  apt:
    packages:
      - libenchant-dev

script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_INSTALL_PREFIX="$HOME" ..
  - make
  - make test
  - make check
  - make install
  - make installcheck

compiler:
  - clang
  - gcc

os:
  - linux
  - osx
