language: c

addons:
  apt:
    packages:
      - libenchant-dev

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - make test
  - make install
  - ../test semordnilap # Check installed binary

compiler:
  - clang
  - gcc
